// Backend API base URL — update this once Render gives you the live URL
// (Render dashboard → your service → URL at the top, looks like
// https://vtc-panda.onrender.com)
export const API_URL = "https://vtc-panda.onrender.com";
